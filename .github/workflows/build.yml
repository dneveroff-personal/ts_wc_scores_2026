name: Build

on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Загрузка энвармента
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # Проверка ошибок
      - name: Run code style check
        run: ./gradlew spotlessCheck

      # Сборка
      - name: Build
        run: ./gradlew build

      # Степень покрытия тестами
      - name: Upload JaCoCo report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: build/reports/jacoco/test/html

      # Создание jar файла
      - name: Upload application
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: application
          path: build/libs/*.jar

      # Докер билд
      - name: Build Docker image
        run: docker build -t ts-wc-scores:${{ github.sha }} .

### Тестовый запуск контейнеров так же можно сделать, но пока не нужно ###
#      - name: Create CI env
#        run: |
#          echo "BOT_TOKEN=fake-token" > .env

#      - name: Start containers
#        run: docker compose up -d

#      - name: Wait
#        run: sleep 30

#      - name: Check application health
#        run: curl --fail http://localhost:8080/actuator/health

#      - name: Stop containers
#        if: always()
#        run: docker compose down